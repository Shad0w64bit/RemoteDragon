GetPacket	// Ссылка на начало пакета
GetHeader	// Ссылка на заголовок
GetData		// Ссылка на данные


SetPosition	// Устанавливает позицию

Position	// Получает указатель на текущую позицию в файле для чтения или записи

GetLength	// Остаток длины относительно позиции
GetPacketLength	// Полная длина пакета

CurrentMove()	// Перемещает указатель относительно текущей позиции